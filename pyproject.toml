[project]
name = "few_shot_keypoints"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "accelerate",
    "airo-dataset-tools",
    "diffusers",
    "draccus",
    "numpy",
    "torch>=2.7.1",
    "torchvision",
    "transformers",
    "xformers",
]

[tool.setuptools]
packages = ["few_shot_keypoints"]
